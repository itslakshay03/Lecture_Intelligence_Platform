# Results

**Empty by design.** No experimental results are included until data collection and evaluation are actually completed (Phase 5+). `raw/` will hold machine-generated JSON/CSV output only (never hand-typed); `processed/` will hold derived summary tables. As of Phase 4, both are empty except for this README.
